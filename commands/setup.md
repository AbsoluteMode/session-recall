---
description: Guided session-recall setup — language, embedder, optional team mode
---

Read and follow the plugin's `setup` skill (skills/setup/SKILL.md): ask the
user the three onboarding questions in chat (interaction language, local or
cloud embedder, solo or team), then run the commands from the skill yourself
and finish with `session-recall health` plus one real search.
